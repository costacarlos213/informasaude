import styled from 'styled-components'

export const HeaderSection = styled.header`
  margin: 2rem;
  text-align: center;

  h1 {
    font-weight: 400;
  }

  p {
    font-size: 1.4rem;
  }

  @media (min-width: 700px) {
    margin: 2vw;
    text-align: left;
  }
`
